[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "18"

# Redirects for single-page application
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
